[pytest]
# NOTE: pytest.ini cannot pick an interpreter. You MUST invoke pytest with the
# Python 3.12 interpreter -- jaclang does not work on 3.11. See JAC_SETUP.md.
#
#   C:\Users\raj_k\AppData\Local\Programs\Python\Python312\python.exe -m pytest

testpaths = tests

markers =
    jac: test requires the Jac toolchain (jaclang / jac.exe). Deselect with -m "not jac".
