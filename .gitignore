__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/
env/
.env
*.egg-info/
.vscode/
.idea/
.DS_Store

# Jac build/state junk. `.jac/` matches at any depth, so it covers both the repo-root
# .jac/data/ session db and jac/.jac/cache/. It does NOT touch jac/*.jac source files.
.jac/

# Data is fetched, not committed (AIS windows are large; geo layers are Tier A/B downloads)
data/
geo/
