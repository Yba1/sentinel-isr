__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/
env/
.env
*.egg-info/
.vscode/
.idea/
.DS_Store

# Raw downloads are fetched, not committed. data/*.py is the replay layer (committed);
# extracted per-pack AIS windows (scenarios/*/ais_window.csv, ~1 MB) are committed too
# so the demo runs without the 400 MB MarineCadastre pull.
/data/raw/
/geo/
